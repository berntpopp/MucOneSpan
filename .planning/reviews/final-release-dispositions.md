# Final Claude Fable 5.1 release review dispositions

Actual Claude Code 2.1.268 session requested `claude-fable-5-1` explicitly and returned successfully. The response's modelUsage includes that model. Verdict: no release-blocking correctness defect found. Review covered the complete source/script/example/docs/packaging/test diff, selected planning records and earlier review responses. The reviewer did not execute tests; actual repository gates are recorded separately.

1. **Medium: review metadata publication. Accepted as evidence.** User explicitly requested committing planning records. Session identifiers, model/cost metadata and local provenance paths are not credentials. Raw responses remain intact for review provenance. Generated data and credentials remain excluded. The final committed-file inventory additionally hashes every staged artifact, including raw review responses omitted from the smaller source snapshot; the original staged patch already included them.
2. **Medium: confidence with unavailable support. Retain explicit scientific contract.** Verified `classify.py` keeps dictionary fit when projection cannot be assessed, while verified absence is penalized. This is deliberate, documented and not a probability. The requested report-level flag already exists: `projection_unavailable` and `localization_ambiguous` have distinct badges, tested by `tests/unit/test_report.py`. Applying an absence penalty to unassessed evidence would change the scientific contract without validation. No sensitivity claim depends on this score.
3. **Low: normal end-to-end assertion. Disclosed limitation, not relaxed test.** The test file and its assertions are unchanged by this release. The new support semantics can make that historical smoke assertion less comprehensive; strict evaluation separately retains every extra template event, including normal controls. The public report explicitly gives the normal false alarm and unresolved counts. Do not interpret this smoke test as validation of normal specificity. A broader raw-template E2E gate remains future work; no tolerance or expected failure was weakened.
4. **Low: dual BAM/FASTQ discovery. Documented.** `discover_input` deliberately rejects multiple candidate inputs rather than silently selecting one; deterministic ambiguity tests already cover it. The simulation guide now explicitly requires disabling optional simulator alignment if it creates both outputs. Alignment-enabled real generation was not tested in this release.
5. **Low: usable BAM wording. Clarified.** The simulation guide now says non-secondary, non-supplementary records, including unmapped records with sequence and quality. Filtering remains unchanged because the caller remaps input records.
6. **Low: driver versus evaluator exit status. Documented.** The benchmarking guide now states that any nonzero caller exit, including typed insufficient evidence, gives a nonzero driver exit; offline evaluation can still complete and score no-calls. No exit-code compatibility change was introduced during final review.
7. **Low: output directory created before input check. Documented.** The configuration guide explains the failure record side effect. This is necessary for observable failures and existing stale-output precedence tests.
8. **Low: changelog heading. Fixed.** Added `### Added` to the 0.11.0 section.

After review, the user requested soft acknowledgment of ideas from Vrbacka et al., without asking for citation. README and related documentation now use that wording; CITATION.cff retains factual metadata with a neutral message. These are prose changes only. Production source and scientific tests have not changed since the reviewed snapshot. Docs and distribution checks are rerun for these final prose changes. No substantive correctness finding remains requiring another implementation review.
